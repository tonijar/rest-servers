Java Spark minimum REST Server for testing purposes.

You can try it with a simple GET request to for example http://localhost:4567/hello/toni

It should return a JSON response such as: {"content":"Hello toni!"}